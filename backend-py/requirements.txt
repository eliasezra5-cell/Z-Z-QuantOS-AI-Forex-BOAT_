fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
bcrypt>=4.1.0
PyJWT>=2.8.0
websockets>=12.0
python-multipart>=0.0.9
redis>=5.0.0
pytest>=8.0.0

# Institutional persistence + queue layer (PostgreSQL/pgvector + Celery)
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0
celery>=5.3.0
cryptography>=42.0.0
feedparser>=6.0.0
praw>=7.0.0

# Observability (additive Module 4.1): OpenTelemetry distributed tracing
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# Phase 4 quantitative analysis (Advanced SMC math + institutional risk models)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Pro quant-stats module (CAPM, OLS, cointegration, unit-root, normality)
statsmodels>=0.14.0

# Pro portfolio-optimizer module (HRP / CVaR allocation + stress-testing)
skfolio>=0.9.0

# Pro forecasting module (Kronos foundation model, NeoQuasar/Kronos-mini)
# Inference deps from the official Kronos repo (shiyu-coder/Kronos
# requirements.txt); note the repo does NOT require `transformers`.
torch>=2.0.0
einops>=0.8.0
huggingface_hub>=0.23.0
safetensors>=0.4.0
tqdm>=4.64.0
